---
phase: 14-objection-handling-thread-engine
plan: 03
type: tdd
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/tests/detect-signals.test.js
autonomous: true
requirements:
  - OBJECTION-01
  - OBJECTION-04
  - OBJECTION-05

must_haves:
  truths:
    - "All 6 objection types (including NONE) are covered by test cases with real-world email phrases"
    - "Priority ordering in detectObjection is verified (ALREADY_HIRED beats PRICING when both keywords present)"
    - "Agency sensitivity correctly fires on job post text and NOT on agency-free text"
    - "Scope framing returns correct enum for all 4 values including UNKNOWN"
    - "Edge cases pass: empty string, null, undefined inputs all return safe defaults"
  artifacts:
    - path: "src/tests/detect-signals.test.js"
      provides: "Unit tests for all three detection functions in detectSignals.js"
      contains: "detectObjection|detectAgencySensitivity|detectScopeFraming"
  key_links:
    - from: "src/tests/detect-signals.test.js"
      to: "src/utils/detectSignals.js"
      via: "require('../utils/detectSignals')"
      pattern: "detectObjection|detectAgencySensitivity|detectScopeFraming"
---

<objective>
TDD tests for detectSignals.js covering all detection functions, all objection types, priority ordering, and edge cases. The functions are pure and deterministic — ideal TDD candidates.

Purpose: These tests verify the detection layer that all prompt augmentation depends on. Catching a false positive in TECHNICAL_Q (e.g., an email mentioning "API" in passing) here prevents bad counter-move injections in production.
Output: src/tests/detect-signals.test.js with full coverage of all three functions
</objective>

<execution_context>
@C:/Users/ASUS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/utils/detectSignals.js
@src/utils/validateReply.js
@src/tests/ooo-detection.test.js
@.planning/phases/14-objection-handling-thread-engine/14-01-SUMMARY.md
</context>

<feature>
  <name>detectSignals — three pure detection functions</name>
  <files>src/utils/detectSignals.js, src/tests/detect-signals.test.js</files>
  <behavior>
    detectObjection(emailText):
    - 'how much does this cost' → 'PRICING'
    - 'what is your rate?' → 'PRICING'
    - 'too expensive for us' → 'PRICING'
    - 'we found someone else already' → 'ALREADY_HIRED'
    - 'we already hired someone' → 'ALREADY_HIRED'
    - 'no agencies please, individuals only' → 'AGENCY'
    - 'comparing proposals from a few freelancers' → 'COMPARISON'
    - 'found someone cheaper, sorry' → 'COMPARISON'
    - 'do you use React or Vue for the frontend?' → 'TECHNICAL_Q'
    - 'we use AWS and need PostgreSQL' → 'TECHNICAL_Q'
    - 'sounds great, let us proceed' → 'NONE'
    - '' → 'NONE'
    - null → 'NONE'
    - Priority: 'we already hired but how much anyway' → 'ALREADY_HIRED' (ALREADY_HIRED beats PRICING)
    - Priority: 'no agencies, we found someone anyway' → 'ALREADY_HIRED' (ALREADY_HIRED beats AGENCY)

    detectAgencySensitivity(jobText):
    - 'Looking for a freelancer only, no agencies' → true
    - 'Prefer solo developer' → true
    - 'Not an agency, individual only' → true
    - 'Experienced developer wanted' → false
    - '' → false
    - null → false
    - 'Agency experience preferred' (mentions agency positively) → false (no agency-restriction keywords)

    detectScopeFraming(emailText):
    - 'split into 3 phases with milestones' → 'PHASES'
    - 'need about 20 hours per week' → 'HOURS'
    - 'fixed price of $2000 total' → 'FIXED'
    - 'hello I need a website' → 'UNKNOWN'
    - '' → 'UNKNOWN'
    - null → 'UNKNOWN'
    - Priority: 'split into phases, roughly 40 hours' → 'PHASES' (PHASES beats HOURS)
  </behavior>
  <implementation>
    Pure function tests. No mocking needed. Use jest describe/it blocks grouped per function.
    Follow the same test structure as ooo-detection.test.js and intent-detection.test.js in the tests directory.
    Use require('../utils/detectSignals') — CommonJS.

    Test file structure:
    - describe('detectObjection') with sub-groups per objection type + priority ordering + edge cases
    - describe('detectAgencySensitivity') with positive, negative, edge cases
    - describe('detectScopeFraming') with all 4 return values + priority + edge cases
  </implementation>
</feature>

<verification>
RED phase: Write tests first, verify they FAIL (detectSignals.js does not exist yet — if it does from Plan 01, temporarily rename to verify failure, then restore).
Actually, since Plan 01 creates detectSignals.js, tests should PASS immediately. The RED→GREEN applies if Plan 01 is being done in the same session; otherwise just run and confirm GREEN.

npm test src/tests/detect-signals.test.js — all tests must pass with 0 failures.
</verification>

<success_criteria>
- All test cases listed in behavior section pass
- Priority ordering verified by at least 2 multi-keyword test cases
- Edge cases (null, empty string) covered for all three functions
- npm test (full suite) passes — no regressions in other test files
- Test file follows CommonJS pattern (require, not import)
</success_criteria>

<output>
After completion, create .planning/phases/14-objection-handling-thread-engine/14-03-SUMMARY.md
</output>
