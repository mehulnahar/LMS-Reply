# Server
PORT=3000
NODE_ENV=development

# Database (Postgres — provided automatically by Railway)
DATABASE_URL=postgresql://user:password@localhost:5432/lms_reply

# Frontend (React — served separately or as static build)
CLIENT_URL=http://localhost:5173
